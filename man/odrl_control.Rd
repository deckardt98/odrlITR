% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{odrl_control}
\alias{odrl_control}
\title{Control computational settings for ODRL}
\usage{
odrl_control(
  tree_depth = 2L,
  tree_split_step = 1L,
  tree_min_node_size = 5L,
  linear_coefficient_bound = 10,
  linear_margin = 1e-04,
  linear_time_limit = 60,
  linear_relative_gap = 0.01,
  linear_require_gap = FALSE,
  svm_kernel = "rbf",
  svm_penalty = c(0.01, 0.1, 1),
  svm_rbf_multiplier = c(0.5, 1, 2),
  svm_radius = 1,
  svm_folds = 3L,
  svm_maxit = 300L,
  relu_hidden_units = c(0L, 8L, 16L),
  relu_decay = c(0.001, 0.01, 0.1),
  relu_folds = 3L,
  relu_restarts = 1L,
  relu_refit_restarts = 3L,
  relu_maxit = 250L,
  relu_selection = c("one_se", "best"),
  score_tolerance = 1e-10,
  seed = 1L,
  tree_backend = "policytree",
  tree_options = list(),
  linear_absolute_gap = NULL,
  linear_node_limit = NULL,
  linear_objective_target = NULL,
  linear_threads = 1L,
  linear_log_to_console = FALSE,
  linear_solver_options = list(),
  svm_polynomial_degree = 2L,
  svm_polynomial_scale = 1,
  svm_polynomial_offset = 1,
  svm_hinge_mode = c("auto", "bounded", "regularized"),
  svm_kernel_function = NULL,
  svm_kernel_args = list(),
  relu_architectures = NULL,
  relu_activation = "relu",
  relu_leaky_slope = 0.01,
  relu_backend = "native",
  relu_preset = NULL,
  relu_backend_options = list()
)
}
\arguments{
\item{tree_depth}{Maximum depth of a direct policy tree. Zero fits a
constant rule, one a stump, and two allows a root split followed by child
splits. The fitted tree may be shallower than this maximum, and search
cost can rise quickly with depth.}

\item{tree_split_step}{Candidate split-step passed to
\code{\link[policytree:policy_tree]{policytree::policy_tree()}}. Values above one reduce computation by
thinning candidate split points and therefore change the searched class.}

\item{tree_min_node_size}{Minimum terminal-node size.}

\item{linear_coefficient_bound}{Absolute MILP coefficient bound.}

\item{linear_margin}{Numerical one-sided margin used to encode a strict
negative affine score. It cannot exceed \code{linear_coefficient_bound}.}

\item{linear_time_limit}{Solver time limit in seconds.}

\item{linear_relative_gap}{Requested relative mixed-integer gap.}

\item{linear_require_gap}{If \code{TRUE}, reject a feasible incumbent unless the
solver proves the requested gap.}

\item{svm_kernel}{Kernel or finite-series specification. Built-in kernels
are \code{"rbf"}/\code{"gaussian"}, \code{"linear"}, and
\code{"polynomial"}/\code{"poly"}. Built-in finite-series shortcuts are
\code{"legendre"}, \code{"fourier"}, \code{"bspline"}, \code{"haar"}, and
\code{"local_polynomial"}; use \code{\link[=odrl_series_kernel]{odrl_series_kernel()}} for detailed controls.
A kernel may also be a function \verb{function(x, y)} or a list with \code{name},
\code{fun}, and optional \code{args}. The caller is responsible for a custom
kernel's symmetry and positive semidefiniteness.}

\item{svm_penalty}{Positive regularization grid for kernel-surrogate fits.}

\item{svm_rbf_multiplier}{Positive multipliers of the median squared
pairwise distance.}

\item{svm_radius}{RKHS-radius grid for bounded-hinge fits. Values must not
exceed one.}

\item{svm_folds}{Number of ODRL-criterion cross-validation folds.}

\item{svm_maxit}{Maximum optimizer iterations for each regularized
kernel-surrogate fit. A nonconverged fit is retried once from its
incumbent with \code{max(3 * svm_maxit, svm_maxit + 100)} iterations.}

\item{relu_hidden_units}{Integer vector of one-hidden-layer widths. Include
zero to add an affine score candidate. This backward-compatible shortcut
is ignored when \code{relu_architectures} is supplied.}

\item{relu_decay}{Nonnegative weight-decay grid.}

\item{relu_folds}{Number of ODRL-criterion cross-validation folds.}

\item{relu_restarts}{Random starts per cross-validation fit.}

\item{relu_refit_restarts}{Random starts for the final ReLU refit.}

\item{relu_maxit}{Maximum optimizer iterations per initial ReLU start. If
no start converges, the best incumbent is retried once with
\code{max(3 * relu_maxit, relu_maxit + 100)} iterations.}

\item{relu_selection}{Select the best criterion candidate or use a one-standard-
error rule favoring smaller networks and stronger decay.}

\item{score_tolerance}{Scores below this absolute value are treated as zero.}

\item{seed}{Reproducible seed used by tuning and optimization.}

\item{tree_backend}{Tree engine. Use \code{"policytree"} or a list containing
\code{name}, \code{fit}, and \code{predict} callbacks. The fit callback receives \code{x},
\code{score}, \code{rewards}, \code{depth}, \code{min_node_size}, \code{split_step}, and \code{options};
the predict callback must return actions in \verb{\{-1,+1\}}.}

\item{tree_options}{Named list of additional options for the tree engine.
For \code{policytree}, dedicated arguments such as \code{depth} cannot be
overridden here.}

\item{linear_absolute_gap}{Optional requested absolute mixed-integer gap.}

\item{linear_node_limit}{Optional branch-and-bound node limit.}

\item{linear_objective_target}{Optional solver objective target.}

\item{linear_threads}{Positive number of HiGHS threads.}

\item{linear_log_to_console}{Whether HiGHS prints its solver log.}

\item{linear_solver_options}{Named list of additional HiGHS options. Options
represented by dedicated arguments cannot be overridden here.}

\item{svm_polynomial_degree}{Positive integer degree grid.}

\item{svm_polynomial_scale}{Positive polynomial-kernel scale.}

\item{svm_polynomial_offset}{Nonnegative polynomial-kernel offset.}

\item{svm_hinge_mode}{\code{"auto"} uses the globally bounded construction for
Gaussian/RBF hinge fits and ordinary regularized hinge fitting otherwise.
\code{"bounded"} explicitly requests the globally bounded RBF construction;
\code{"regularized"} enables ordinary hinge fitting with any supported score
class.}

\item{svm_kernel_function}{Function used with \code{svm_kernel = "custom"}.}

\item{svm_kernel_args}{Named arguments passed to a custom kernel function.}

\item{relu_architectures}{Optional list of hidden-layer width vectors. Use
\code{integer()} for an affine score, \code{8L} for one hidden layer, and
\code{c(16L, 8L)} for two hidden layers.}

\item{relu_activation}{Activation grid: \code{"relu"}, \code{"leaky_relu"}, \code{"tanh"},
\code{"sigmoid"}, or \code{"linear"}.}

\item{relu_leaky_slope}{Negative-side slope for leaky ReLU.}

\item{relu_backend}{\code{"native"}, \code{"nnet"}, or a list containing \code{name},
\code{fit}, and \code{predict} callbacks for an external neural-network engine.
The \code{"nnet"} backend supports logistic loss with an affine score or one
sigmoid hidden layer. The fit
callback receives \code{x}, \code{score}, \code{architecture}, \code{activation}, \code{decay},
\code{loss}, \code{restarts}, \code{maxit}, \code{seed}, and \code{leaky_slope}. The predict
callback is called as \code{predict(model, newx)} and must return one finite
numerical score per row.}

\item{relu_preset}{Optional quick-start neural candidate grid. Use
\code{"affine"}, \code{"fast"}, \code{"standard"}, \code{"flexible"}, or \code{"nnet"}.
A preset supplies defaults only: explicitly supplied fine-grained neural
controls take precedence. Presets define candidates selected by
policy-value cross-validation; they are not ensembles. Historical
\verb{relu_*} names cover the generic neural learner for backward
compatibility.}

\item{relu_backend_options}{Named backend-specific options. For
\code{relu_backend = "nnet"}, supported entries are \code{skip}, \code{rang},
\code{MaxNWts}, \code{abstol}, \code{reltol}, \code{trace}, and \code{probability_epsilon}.}
}
\value{
An object of class \code{odrl_control}.
}
\description{
Control computational settings for ODRL
}
