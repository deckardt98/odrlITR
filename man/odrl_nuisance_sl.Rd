% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nuisance.R
\name{odrl_nuisance_sl}
\alias{odrl_nuisance_sl}
\title{Cross-fitted Super Learner nuisances for ODRL}
\usage{
odrl_nuisance_sl(
  x,
  a,
  y,
  folds = 5L,
  sl.library = c("SL.mean", "SL.glm", "SL.glmnet"),
  sl.library.pi = sl.library,
  sl.library.m = sl.library,
  inner_folds = 5L,
  seed = 1L,
  positive = NULL,
  propensity_bounds = NULL,
  known_pi = NULL,
  known_e = NULL,
  verbose = FALSE,
  env = parent.frame(),
  fold_id = NULL
)
}
\arguments{
\item{x}{Covariate matrix or data frame.}

\item{a}{Binary treatment, coded as \verb{\{0,1\}}, \verb{\{-1,+1\}}, logical, or a
two-level factor.}

\item{y}{Numeric outcome.}

\item{folds}{Number of outer cross-fitting folds. Ignored when \code{fold_id} is
supplied. Automatically generated folds are treatment-stratified, and
their total sizes differ by at most one row.}

\item{sl.library}{Character vector or list understood by
\code{\link[SuperLearner:SuperLearner]{SuperLearner::SuperLearner()}}.}

\item{sl.library.pi}{Optional library for the propensity. Defaults to
\code{sl.library}.}

\item{sl.library.m}{Optional library for the marginal outcome. Defaults to
\code{sl.library}.}

\item{inner_folds}{Super Learner cross-validation folds within each outer
training sample.}

\item{seed}{Reproducibility seed.}

\item{positive}{For a factor or character treatment, the level representing
treatment \code{+1}. By default, the second factor level is positive.}

\item{propensity_bounds}{Optional numerical safeguard. \code{NULL} retains the
Super Learner predictions exactly.}

\item{known_pi}{Optional scalar or vector with one known propensity per row.
This skips propensity fitting while retaining cross-fitting for the
marginal outcome regression.}

\item{known_e}{Optional known \code{E(A|X)} on the \verb{\{-1,+1\}} scale. Supply at
most one of \code{known_pi} and \code{known_e}.}

\item{verbose}{Passed to \code{\link[SuperLearner:SuperLearner]{SuperLearner::SuperLearner()}}.}

\item{env}{Environment in which custom Super Learner wrappers are resolved.}

\item{fold_id}{Optional outer fold identifiers supplied for each row.
Labels may be numeric, character, or factor and are normalized internally.
Every resulting training set must contain at least two observations and
must retain at least two observations from each treatment arm when the
propensity is estimated.}
}
\value{
An \code{odrl_nuisance} object containing aligned out-of-fold
predictions. Its \code{fits} component records the libraries, coefficients,
actual numbers of inner folds (possibly capped) and captured warnings for
every outer fold; \code{warnings} is the unique aggregate warning ledger.
}
\description{
Fits the propensity \eqn{P(A=+1\mid X)} and pooled marginal outcome
regression \eqn{E(Y\mid X)} on outer training folds and predicts held-out
rows. Super Learner's internal cross-validation is separate from this outer
cross-fitting layer.
}
