% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{odrl}
\alias{odrl}
\title{Fit an orthogonal double-residual treatment rule}
\usage{
odrl(
  x,
  a,
  y,
  learner = c("tree", "linear", "svm", "relu"),
  loss = NULL,
  nuisance = NULL,
  nuisance_folds = 5L,
  sl.library = c("SL.mean", "SL.glm", "SL.glmnet"),
  sl.library.pi = sl.library,
  sl.library.m = sl.library,
  sl_inner_folds = 5L,
  known_pi = NULL,
  known_e = NULL,
  propensity_bounds = NULL,
  positive = NULL,
  control = odrl_control(),
  sl_verbose = FALSE,
  sl_env = parent.frame(),
  nuisance_fold_id = NULL
)
}
\arguments{
\item{x}{Covariate matrix or data frame.}

\item{a}{Binary treatment. Numeric values may be \verb{\{0,1\}} or \verb{\{-1,+1\}}.}

\item{y}{Numeric outcome, with larger values preferred.}

\item{learner}{One of \code{"tree"}, \code{"linear"}, \code{"svm"}, or \code{"relu"}.}

\item{loss}{\code{"exact"} for tree/linear; \code{"hinge"}, \code{"logistic"}, or
\code{"squared_hinge"} for SVM/neural learners; or a custom differentiable
signed-margin loss. SVM custom losses use a list with \code{value} and
\code{gradient} functions. Neural custom losses may use the same list or a
function returning \code{list(loss, gradient)}. If \code{NULL}, direct learners use
exact loss and surrogate learners use hinge loss. The caller is
responsible for the mathematical properties of a custom loss, including
convexity when a convex kernel-surrogate problem is intended.}

\item{nuisance}{\code{NULL}/\code{"superlearner"} for cross-fitted Super Learner,
\code{"parametric"}/\code{"glm"} for cross-fitted logistic treatment and linear
outcome models, an \code{\link[=odrl_nuisance_user]{odrl_nuisance_user()}} object, a list containing \code{m}
plus \code{pi} or \code{e}, or a function \verb{function(x, a, y)} returning one of those
objects.}

\item{nuisance_folds}{Number of outer nuisance cross-fitting folds.}

\item{sl.library}{Library passed to \code{\link[=odrl_nuisance_sl]{odrl_nuisance_sl()}}.}

\item{sl.library.pi}{Optional propensity-specific Super Learner library.}

\item{sl.library.m}{Optional marginal-outcome-specific Super Learner library.}

\item{sl_inner_folds}{Super Learner's inner cross-validation folds.}

\item{known_pi, known_e}{Optional known propensity on the probability or
\code{E(A|X)} scale. This enables randomized-trial use while cross-fitting \code{m}.}

\item{propensity_bounds}{Optional two-element numerical safeguard. \code{NULL}
performs no propensity clipping because the ODRL score does not divide by
the propensity.}

\item{positive}{For factor/character treatment, the level representing
treatment \code{+1}. The second factor level is used by default.}

\item{control}{Computational settings from \code{\link[=odrl_control]{odrl_control()}}.}

\item{sl_verbose}{Whether Super Learner prints progress.}

\item{sl_env}{Environment for custom Super Learner wrappers.}

\item{nuisance_fold_id}{Optional row-aligned user-specified outer-fold
identifiers. When omitted, built-in folds are treatment-stratified and
balanced to differ in total size by at most one row. User folds must leave
at least two observations in every outer training set and, when the
propensity is estimated, at least two observations from each arm.}
}
\value{
An object of class \code{odrl_fit}, a list with the following principal
components:
\describe{
\item{\code{policy}}{The fitted second-stage policy, including its selected
tuning values and learner-specific diagnostics.}
\item{\code{nuisance}}{The aligned nuisance predictions and first-stage
diagnostics used to construct the score.}
\item{\code{score}}{Raw and normalized double-residual scores, their scale,
and degeneracy indicators.}
\item{\code{training}}{Fitted treatment actions and the empirical ODRL
criterion evaluated with the unscaled score.}
\item{\code{blueprint}}{The covariate encoding retained for safe prediction.}
\item{\code{treatment_map}}{The map between internal \verb{\{-1,+1\}} actions and
the treatment coding supplied by the user.}
}
The object also records \code{call}, \code{learner}, \code{loss}, \code{control}, sample
dimensions \code{n} and \code{p}, and elapsed \code{runtime}. Use \code{\link[=predict.odrl_fit]{predict.odrl_fit()}},
\code{\link[=fitted.odrl_fit]{fitted.odrl_fit()}}, \code{\link[=coef.odrl_fit]{coef.odrl_fit()}}, and \code{\link[=summary.odrl_fit]{summary.odrl_fit()}} rather
than relying on learner-specific internals.
}
\description{
\code{odrl()} estimates or accepts the two ODRL nuisances, constructs pooled
out-of-fold double-residual scores, and fits one policy. The direct learners
optimize the empirical binary objective over a bounded-margin affine class
or the candidate shallow-tree class searched by \code{policytree}.
Surrogate learners support hinge, logistic, squared-hinge, and custom
differentiable margin losses over configurable RKHS or neural-network score
classes.
}
\examples{
data <- odrl_simulate(250, boundary = "tree", seed = 4)
nuisance <- odrl_nuisance_user(
  m = data$m, pi = data$pi, source = "known simulation truth",
  out_of_fold = TRUE
)
if (requireNamespace("policytree", quietly = TRUE)) {
  fit <- odrl(data$x, data$a, data$y, learner = "tree",
              nuisance = nuisance)
  predict(fit, data$x[1:5, ])
}
}
